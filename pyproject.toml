[project]
name = "marketinghub"
authors = [
    { name = "Henry Diaz", email = "henry1395.hd@gmail.com"}
]
description = "Gestion de marketing (Meta Ads, WhatsApp, CTWA, campanas)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "marketinghub"
